# dnsmasq 配置

## 1. 主配置文件

文件：

- `/etc/dnsmasq.d/router.conf`

内容：

```conf
# 把 dnsmasq 日志写到单独文件，便于排障
log-facility=/var/log/dnsmasq.log
log-async=20

# 只监听 lan0，避免误在其他接口提供服务
interface=lan0
bind-dynamic
listen-address=192.168.1.1

# 基础安全与行为约束
domain-needed
bogus-priv
no-resolv
strict-order
no-round-robin
cache-size=0
local-service
rebind-localhost-ok
stop-dns-rebind
dhcp-authoritative
log-dhcp

domain=example.internal
local=/example.internal/
expand-hosts
host-record=router.example.internal,192.168.1.1,fd00:1234:5678:1::1

# 自建DNS服务器
# 上游 DNS：mini-ppdns / PaoPaoDNS 的 IPv4 与 IPv6 地址
server=192.168.1.250
server=192.168.1.2
server=fd00:1234:5678:1::53
server=fd00:1234:5678:1::54

# IPv4 DHCP
# 动态地址池
dhcp-range=192.168.1.100,192.168.1.200,255.255.255.0,12h
# 默认网关与 DNS 都指向主路由
dhcp-option=option:router,192.168.1.1
dhcp-option=option:dns-server,192.168.1.1
dhcp-option=option:domain-search,example.internal
# fakeip 流量额外下发类静态路由
dhcp-option=option:classless-static-route,7.0.0.0/8,192.168.1.4
# 某些设备按 tag 下发额外公共 DNS
dhcp-option=tag:aliyun_google_dns,option:dns-server,223.5.5.5,8.8.8.8

# IPv6: stateful + SLAAC
# 开启 RA，并显式下发 MTU
enable-ra
ra-param=lan0,mtu:1500,30,180
# 一套来自当前 GUA 前缀的动态地址
dhcp-range=::100,::1ff,constructor:lan0,slaac,64,12h
# 一套固定 ULA 前缀上的地址
dhcp-range=fd00:1234:5678:1::100,fd00:1234:5678:1::1ff,slaac,64,12h

# 其余 DHCP 行为调整
dhcp-broadcast=tag:needs-broadcast
dhcp-ignore-names=tag:dhcp_bogus_hostname
dhcp-leasefile=/var/lib/misc/dnsmasq.leases
```

## 2. IPv4 静态地址

文件：

- `/etc/dnsmasq.d/dnsmasq.staticv4.conf`

内容：

```conf
# 这台 DNS 节点会额外拿到公共 DNS 作为补充
dhcp-host=02:00:00:00:00:16,set:aliyun_google_dns,192.168.1.2,dns-global,2h
# 这台上游节点固定拿到 192.168.1.250
dhcp-host=02:00:00:00:00:18,192.168.1.250,ap-upstream,2h
```

## 3. IPv6 静态地址

文件：

- `/etc/dnsmasq.d/dnsmasq.staticv6.conf`

当前状态：

- 空文件
- 尚未正式启用静态 IPv6 地址保留

## 4. 说明

- 当前 `dnsmasq` 同时下发：
  - 一套动态 GUA
  - 一套固定 ULA
- 当前没有在 `router.conf` 中显式下发 `option6:dns-server`
- 当前上游 DNS 服务器说明见：
  - [11.内网辅助节点说明](./11.内网辅助节点说明.md)
- 对于带 `set:aliyun_google_dns` 的设备，会额外拿到：
  - `223.5.5.5`
  - `8.8.8.8`

## 5. 语法检查

```bash
dnsmasq --test
```

当前结果应为：

```text
dnsmasq: syntax check OK.
```

